# node
node_modules/
*.log
.npm
.pnpm-store/
dist/

# rust / anchor / cargo (any nested target)
**/target/
**/*.rs.bk
.anchor/
test-ledger/

# circom artifacts
*.r1cs
*.sym
*.wasm
*.zkey
*.vkey
*.ptau
*.wtns
*.dat
*.so
*.cpp
*.hpp
*.asm
witness.wtns
proof.json
public.json
verification_key.json

# environment / secrets
.env
.env.local
.env.*.local
keypair.json
!example-keypair.json
!target/deploy/*-keypair.json

# editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# python
__pycache__/
*.pyc

# zkx-live specific
circuits/deps/
circuits/build/
circuits/ptau/

# Proprietary zkX prover service — kept local, not in the public repo.
# The public stack is prover-agnostic; any Groth16 prover that produces valid
# proofs over our circuits works in this slot.
prover/

# Old experiments not part of the demo (kept locally, not pushed)
prover-service/
rabbitsnark-py/
verifier-test/
sdk/package-lock.json
rabbitsnark-fix.patch

# Track these specifically
!programs/*/keypair.json

# nextjs build cache
**/.next/

# Per-app build caches
**/tsconfig.tsbuildinfo

# Local design exploration drafts (not part of the shipped site)
apps/site/THEME*_KR.md
sketches/

# Vercel CLI link state (already self-ignored, listed for clarity)
.vercel

